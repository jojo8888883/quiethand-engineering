# 本次打包验证

2026-09-19，仅验证工程包的可携带性，没有重跑视频模型或改变科学结果。

## 已执行

- 将源码 zip 解压到原工作区以外的临时目录。
- 从解压目录运行 `python3 examples/reuse_demo.py`：成功，输出物体身份绑定、显式未知、带来源的证据和合成约束区间。
- 使用已有 Python 3.13 环境运行 `python -m unittest discover -s tests -v`：**56 / 56 通过，约 6 秒**。
- 在解压目录离线构建 wheel：成功，无网络或依赖下载。
- 将 wheel 离线安装到另一个独立目录，用隔离 Python 进程 import 核心包并读取随包 JSON schema：成功。
- 导出文件清单检查：没有原始媒体、数组数据、CAD、权重、用户原始评语目录；指定字面量凭证检查未发现命中。
- 本地资产索引和 README 的 Markdown 文件链接检查：没有缺失目标。

测试覆盖身份绑定、缺项和来源记录、约束数学、相对运动、投影/mask 一致性、合成几何修正，以及示例本身。已有测试中的一个正则字符串在新 Python 下有 SyntaxWarning，测试仍通过；本轮没有修改历史实现来消除此类非阻塞提示。

## 本次不代表什么

不是重新验证 Qwen/HaWoR/SAM2/FoundationPose 效果；不是在新设备上完整重建 GPU 环境；不是实测相近项目。随包脚本仍分为可直接调用函数和需原数据布局接入的实验配方。

原视频、用户评语、模型结果和失败记录留在原位置。以上是初次打包时的验证；随后独立文件夹建立本地 Git 版本，尚未对外发布。
